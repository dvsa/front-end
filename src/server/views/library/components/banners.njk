{% extends 'layouts/library.layout.njk' %}

{% set pageTitle = 'MOT Library - Banners' %}

{% block content %}

  {{ components.heading(text='Default banner', tag='h2', size='medium') }}

  {{ components.dvsaBanner(mainText='This is a draft copy of the MOT manual', secondaryText='The final version is due to be released in XYV 2018') | prism }}

  {{ components.heading(text='Info banner', tag='h2', size='medium') }}

  {{ components.dvsaBanner(mainText='This is a draft copy of the MOT manual', secondaryText='The final version is due to be released in XYV 2018', type='info') | prism }}

  {{ components.heading(text='Error banner', tag='h2', size='medium') }}

  {{ components.dvsaBanner(mainText='Your account is locked', secondaryText='You can try to sign in again after 30 minutes', type="error") | prism }}

  {{ components.heading(text='Confirmation banner', tag='h2', size='medium') }}

  {{ components.dvsaBanner(mainText='Thank you', secondaryText='Your report has been sent', type="confirmation") | prism }}

{% endblock %}