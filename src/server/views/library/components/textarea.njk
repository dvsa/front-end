{% extends 'layouts/library.layout.njk' %}

{% set pageTitle = 'GDS Library - Textarea' %}

{% block content %}

  {{ components.heading(text='Textarea', tag='h2', size='medium') }}
    
  {{ components.textarea(label="Why can't you provide a National Insurance number?", id='textarea') | prism }}

  {{ components.heading(text='Textarea with hint', tag='h2', size='medium') }}
  {{ components.textarea(label="Why can't you provide a National Insurance number?", hint='Hint goes here', id='textarea') | prism }}

  {{ components.heading(text='Textarea with error', tag='h2', size='medium') }}
  {{ components.textarea(label="Label", labelBold=true, id='textarea', hint='Hint goes here', group=true, error='Error message goes here') | prism }}

{% endblock %}