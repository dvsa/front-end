{% extends 'layouts/library-mts.layout.njk' %}

{% set pageTitle = 'MOT Library - Vehicle Information' %}

{% block content %}

  {{ components.heading(
    text='Vehicle Information',
    tag='h1',
    size='large'
  ) }}

  {{ components.paragraph(
    text="Features in Find a Vehicle and Test Summary views in addition to a varient on the VTS Home page.") }}

  {{ components.heading(
    text='Vehicle search results',
    tag='h2',
    size='medium'
  ) }}

  {{ components.paragraph(
    text="Each result is stacked, with the 'Select vehicle' button. Bottom border separator will oinly show if there is more than one result.") }}

  {% call components.displayCode() %}
    {{ components.vehicleInfo(vehicleName='Peugeot 3008 SUV', reg='TH10 CZR', vin='GAULAGAAAAA495118', color='red', selectVehicle=true) }}
    {{ components.vehicleInfo(vehicleName='Peugeot 3008 SUV', reg='RH15 ERL', vin='RRULAGAAFAA495138', color='blue', selectVehicle=true) }}
  {%- endcall %}

  {{ components.heading(
    text='Vehicle Information without search',
    tag='h3',
    size='medium'
  ) }}
  
  {% call components.displayCode() %} 
  {{ components.vehicleInfo(vehicleName='Peugeot 3008 SUV', reg='TH10 CZR', vin='GAULAGAAAAA495118', color='red', selectVehicle=false) }}
  {%- endcall %}

{{ components.heading(
  text='Vehicle Information on Home page',
  tag='h3',
  size='medium'
) }}

{% call components.displayCode() %} 
  {{ components.vehicleInfo(vehicleName='Peugeot 3008 SUV', reg='TH10 CZR', vin='GAULAGAAAAA495118', color='red', selectVehicle=false, home='true') }}  
{%- endcall %}

  {{ components.heading(
    text='Vehicle information',
    tag='h1',
    size='medium'
  ) }}

  {{ components.paragraph(
    text='Example of vehicle search showing vehicle information prior to selecting vehicle for test. Makes it easy for testers to verify the vehicle presented for test is the one being selected.',
    lead=true
  ) }}
  
  {{ components.heading(
    text='Coded examples',
    tag='h3',
    size='small'
  ) }}
  
  {{ components.link(text='Find a Vehicle', paragraph=true, url='/prototypes/responsive-mot/vehicle-result', back=false) }}
  {{ components.link(text='Confirm Vehicle and start test', paragraph=true, url='/prototypes/responsive-mot/confirm-vehicle-details', back=false) }}
 

{% endblock %}