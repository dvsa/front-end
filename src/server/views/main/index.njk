{% extends 'layouts/default.layout.njk' %}

{% set pageTitle = 'MOT Design System' %}

{% block content %}

  {{ components.pageHeading('DVSA Front-end', 'Build v' + repoPackageJSON.version) }}

  {% call components.gridRow() %}

    {% call components.columnOneThird() %}

      {{ components.heading(text='Styleguide', tag='h2', size='medium', url='/library') }}
      {{ components.paragraph(text='DVSA components library available for all DVSA applications.') }}
      
    {%- endcall %}

    {% call components.columnOneThird() %}

      {{ components.heading(text='Prototypes', tag='h2', size='medium', url='/prototypes') }}
      {{ components.paragraph(text='All DVSA prototypes and user journeys.') }}
      
    {%- endcall %}

  {%- endcall %}

  {% call components.gridRow() %}

    {% call components.columnTwoThirds() %}

      {{ components.heading(text='About the Design System', tag='h2', size='medium') }}
      {{ components.paragraph(text='The MOT Testing Service Design System generates versioned images, 
      CSS and JavaScript for the MOT testing service as well as providing a platform to document patterns, 
      design principles, user insight and do rapid prototyping.') }}

    {% endcall %}

  {% endcall %}

{% endblock %}