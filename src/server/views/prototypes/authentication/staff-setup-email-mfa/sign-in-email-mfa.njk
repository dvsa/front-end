{% extends 'layouts/default.layout.njk' %}

{% set pageTitle = 'Set up account security' %}

{% block content %}

  {% call components.gridRow() %}

    {% call components.columnFull() %}

    <header class="content-header" style="margin-top: 35px;" >
      <span class="govuk-caption-xl">Sign in</span>
      <h1 class="govuk-heading-xl">Your 6 digit security code</h1>
    </header>

      <div class="text u-clearfix">
        {{ components.paragraph(text='Enter the 6 digit security code that you received via email ', lead=true) }}

       <div class="form" style="margin-top:30px">
                      <label class="form-label" for="code">Code in email</label>
                      <input type="code" class="form-control" id="code">
                    </div>

      </form>

      <div class="u-margin-top-30 u-margin-bottom-30">
      <a href="#">Send code again</a>
      </div>

<div class="text">
        <h3 class="heading-medium">What happens next?</h3>
        <p>Next time you sign into the MOT testing service you will be asked for:</p>
  
  {{ components.list(
    items=[
    { text: 'your username' },
    { text: 'your password' },
    { text: 'the 6 digit code sent to you via email' }
    ],
    type='bullet'
    ) }}

        <p>You will need to use the code once a day.</p>     
       

    </div>  
      <div class="u-margin-top-10 u-margin-bottom-30">
        {{ components.button(text='Sign in', url='/prototypes/authentication/special-notice-v3/2FA/index') }}
      </div>

<a href="/prototypes/authentication/staff-setup-email-mfa/index">Cancel and return to sign in </a>
     
    {%- endcall %}

  {%- endcall %}
  
{% endblock %}